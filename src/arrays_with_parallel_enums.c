#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(int argc,char **argv){
    enum COLORS { red, blue, green, max };
    char *cstring[max] = { "red", "blue", "green" };

    return 0;

}
